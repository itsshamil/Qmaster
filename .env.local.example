# Copy this file to .env.local and fill in values
# If using a local Supabase (supabase start), set VITE_SUPABASE_URL to the local URL (usually http://localhost:54321) and VITE_SUPABASE_PUBLISHABLE_KEY to your anon/public key.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional server-side key (do NOT commit to client-side builds). Use only in server contexts.
# SUPABASE_SERVICE_ROLE_KEY=
